# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Test coverage (vitest v8) — CI 가 artifact 로 업로드
coverage

# Node dependencies
node_modules
package-lock.json

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Claude Code local data (agent-memory, worktrees) — never commit
.claude/

# Nuxt auto-managed (setups.* etc.)
.nuxtrc

# test coverage output
coverage/

# Playwright e2e artifact + screenshot 산출물 (docs/screenshots/ 로 별도 archive)
test-results/
playwright-report/
e2e/output-screenshots/
